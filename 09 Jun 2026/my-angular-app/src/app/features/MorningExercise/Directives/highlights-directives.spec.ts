import { HighlightsDirectives } from './highlights-directives';

describe('HighlightsDirectives', () => {
  it('should create an instance', () => {
    const directive = new HighlightsDirectives();
    expect(directive).toBeTruthy();
  });
});
